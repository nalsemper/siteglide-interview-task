query get_todo_category($id: ID!) {
  records(
    per_page: 1
    page: 1
    filter: {
      table: { value: "module_987" }
      id: { value: $id }
    }
  ) {
    results {
      id
      properties
    }
  }
}
